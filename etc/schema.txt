create keyspace agon with replication = {'class' : 'SimpleStrategy', 'replication_factor' : 3 };

create table actions ( player_id int, event varchar, event_type varchar, event_time timeuuid, value varchar, primary key (player_id, event_time, event));